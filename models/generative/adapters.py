"""Reserved for explicit network adapters.

Future scope: conditioning inputs, network-specific time encoding and output
format normalization. Trainable conditioning modules must remain inside the
module wrapped by DistributedDataParallel. No adapter is implemented here yet.
"""
